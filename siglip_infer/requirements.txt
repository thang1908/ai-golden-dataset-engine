# Runtime dependencies for the SigLIP inference bundle.
#
# CPU by default. For GPU inference, replace onnxruntime with onnxruntime-gpu
# (they conflict; do not install both) and pass --device cuda:
#
#     pip uninstall -y onnxruntime && pip install onnxruntime-gpu
#
# Pinned to the versions this bundle was validated against on 2026-09-03.
numpy>=2.0,<3
onnxruntime>=1.20,<2
pillow>=11,<12
tokenizers>=0.21,<1
ftfy>=6.3,<7
